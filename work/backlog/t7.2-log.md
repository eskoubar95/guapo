# Task t7.2 — Verificer Payload E2E (GUA-53)

**Workspace:** apps/cms  
**Branch:** feature/m7-backend-payload-medusa  
**Linear:** GUA-53 (In Progress)

## E2E-checkliste (manuel verificering)

Kør CMS lokalt: `pnpm -C apps/cms dev` → http://localhost:3001/admin

### 1. Login
- [ ] Åbn `/admin` → redirect til login eller create-first-user.
- [ ] Log ind med eksisterende bruger (eller opret første bruger).
- [ ] Dashboard vises uden runtime-fejl.

### 2. Collections (CRUD)
For hver: åbn listen → opret ny → udfyld påkrævede felter → gem → genindlæs side og bekræft at data er der.

| Collection        | Admin-URL                              | Check |
|-------------------|----------------------------------------|-------|
| Users             | `/admin/collections/users`             | [ ]   |
| Pages             | `/admin/collections/pages`             | [ ]   |
| Articles          | `/admin/collections/articles`          | [ ]   |
| Media             | `/admin/collections/media`             | [ ]   |
| Product Guidance  | `/admin/collections/product-guidance`  | [ ]   |

### 3. Globals
Åbn hver global → rediger felter → gem → genindlæs og bekræft.

| Global    | Admin-URL                   | Check |
|-----------|-----------------------------|-------|
| Navigation| `/admin/globals/navigation` | [ ]   |
| Footer    | `/admin/globals/footer`     | [ ]   |
| Homepage  | `/admin/globals/homepage`   | [ ]   |

### 4. Persistence (Supabase)
Efter redigering: verificer i Supabase (SQL eller Table Editor) at schema `payload` indeholder opdaterede rækker i `payload.users`, `payload.pages`, `payload.articles`, `payload.media`, `payload.product_guidance`, `payload.navigation`, `payload.footer`, `payload.homepage`.

---

## Schema-verifikation (Supabase)

Kørt 2026-02-03 via Supabase MCP `execute_sql` mod schema `payload`:

| Tabel            | Antal rækker | Bemærkning        |
|------------------|--------------|-------------------|
| users            | 1            | Fra create-first-user (t7.1) |
| pages            | 0            | Tabel klar til CRUD |
| articles         | 0            | Tabel klar til CRUD |
| media            | 0            | Tabel klar til CRUD |
| product_guidance | 0            | Tabel klar til CRUD |
| navigation       | 0            | Tabel klar til CRUD |
| footer           | 0            | Tabel klar til CRUD |
| homepage         | 0            | Tabel klar til CRUD |

Konklusion: Schema `payload` er til stede og skrivbart; collections og globals matcher config (payload.config.ts).

---

## Log

- **2026-02-03** — Task start (SDD /task/start). Linear GUA-53 sat til "In Progress"; kommentar tilføjet. tasks.local.md t7.2 status opdateret til "in progress". Git-state initialiseret på feature/m7-backend-payload-medusa.
- **2026-02-03** — E2E-checkliste tilføjet (login, 5 collections, 3 globals, persistence). Supabase schema verificeret: alle 8 tabeller findes; users=1, øvrige=0. Config (collections/globals) matcher payload.config.ts.
